services:
  db:
    restart: unless-stopped

  api:
    restart: unless-stopped

    env_file:
      - apps/api/.env.production

  web:
    restart: unless-stopped
    
    env_file:
      - apps/web/.env.production
